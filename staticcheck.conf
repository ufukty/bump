checks = [
    "all",

    # QF: quickfixes
    "-QF1001",  # apply De Morgan's law
    "-QF1005",  # expand call to math.Pow
    "-QF1006",  # lift if+break into loop condition
    "-QF1007",  # merge conditional assignment into variable declaration
    "-QF1008",  # omit embedded fields from selector expression
    "-QF1011",  # omit redundant type from variable declaration

    # S: simplifications
    "-S1002",   # omit comparison with boolean constant
    "-S1005",   # drop unnecessary use of the blank identifier
    "-S1006",   # use 'for {}' for infinite loops
    "-S1008",   # simplify returning boolean expression
    "-S1011",   # use a single append to concatenate two slices
    "-S1016",   # use a type conversion instead of copying struct fields
    "-S1021",   # merge variable declaration and assignment
    "-S1025",   # don't use fmt.Sprintf("%s", x) unnecessarily
    "-S1029",   # range over the string directly

    # SA: static analysis / bug-finding
    "-SA1000",  # invalid regular expression
    "-SA1002",  # invalid format in time.Parse
    "-SA1003",  # unsupported argument to encoding/binary
    "-SA1007",  # invalid URL in net/url.Parse
    "-SA1010",  # FindAll called with n == 0
    "-SA1011",  # invalid UTF-8 passed to strings functions
    "-SA1014",  # non-pointer passed to Unmarshal/Decode
    "-SA1015",  # leaking time.Tick
    "-SA1017",  # unbuffered channel with signal.Notify
    "-SA1018",  # strings.Replace called with n == 0
    "-SA1020",  # invalid host:port to net.Listen
    "-SA1021",  # using bytes.Equal to compare net.IP
    "-SA1023",  # modifying the buffer in an io.Writer
    "-SA1024",  # cutset with duplicate characters
    "-SA1025",  # misuse of (*time.Timer).Reset return value
    "-SA1026",  # marshaling channels or functions
    "-SA1027",  # atomic access not 64-bit aligned
    "-SA1028",  # sort.Slice on non-slice
    "-SA1029",  # inappropriate key in context.WithValue
    "-SA1030",  # invalid argument to a strconv function
    "-SA1031",  # overlapping byte slices passed to an encoder
    "-SA1032",  # wrong argument order to errors.Is
    "-SA2002",  # FailNow/SkipNow in a goroutine
    "-SA2003",  # deferred Lock instead of Unlock
    "-SA4005",  # field assignment never observed (pointer receiver?)
    "-SA4006",  # value never read before being overwritten
    "-SA4008",  # loop-condition variable never changes
    "-SA4009",  # argument overwritten before first use
    "-SA4010",  # result of append never observed
    "-SA4012",  # comparing against NaN
    "-SA4015",  # math.Ceil etc. on int-derived floats
    "-SA4017",  # discarding return of a side-effect-free function
    "-SA4018",  # self-assignment
    "-SA4023",  # impossible comparison of interface with untyped nil
    "-SA4031",  # checking a never-nil value against nil
    "-SA5000",  # assignment to nil map
    "-SA5002",  # empty 'for {}' spins
    "-SA5005",  # finalizer references the finalized object
    "-SA5007",  # infinite recursive call
    "-SA5010",  # impossible type assertion
    "-SA5011",  # possible nil pointer dereference
    "-SA5012",  # odd-sized slice to function expecting even
    "-SA6000",  # regexp.Match in a loop
    "-SA6001",  # missed map-by-byte-slice optimization
    "-SA6002",  # storing non-pointer values in sync.Pool
    "-SA6003",  # converting a string to []rune before ranging
    "-SA9001",  # defers in range loops
    "-SA9003",  # empty if/else branch
    "-SA9005",  # marshaling a struct with no public fields
    "-SA9007",  # deleting a directory that shouldn't be deleted
    "-SA9008",  # else branch of a type assertion reading the wrong value

    # ST: stylecheck
    "-ST1*",
]